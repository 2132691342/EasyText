# ── IDE / Editor ──────────────────────────────────────────
.idea/
.vscode/
*.iml
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Build output ──────────────────────────────────────────
easy-text.exe
*.exe
# 发布产物显式纳入版本控制：主程序 + NSIS 安装包
!build/bin/EasyText.exe
!build/bin/EasyText-amd64-installer.exe

# ── Go ────────────────────────────────────────────────────
# Wails builds produce these, keep source only
# /build/ 目录下的 appicon 和 README 可以提交，bin 目录忽略
# go.sum 需要提交，它是 lockfile

# ── Frontend / Node ───────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/package.json.md5

# ── Wails generated (re-generated by `wails dev`/`build`) ──
# v2.1：提交由 scripts/gen-bindings.cjs 生成的真实桥接（window.go 调用，
# 与 wails CLI 生成物等价），解决 dev/build 前置 vue-tsc 时 bindings
# 尚未生成的鸡生蛋死循环；wails CLI 本地重新生成时会覆盖，内容等价。

# ── OS junk ───────────────────────────────────────────────
Desktop.ini
$RECYCLE.BIN/

# ── Env / Secrets ─────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key

# ── Logs / Temp ───────────────────────────────────────────
*.log
tmp/
temp/
.tmp-sel-test/