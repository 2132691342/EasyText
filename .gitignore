# ── IDE / Editor ──────────────────────────────────────────
.idea/
.vscode/
*.iml
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Build output ──────────────────────────────────────────
easy-text.exe
*.exe

# ── Go ────────────────────────────────────────────────────
# Wails builds produce these, keep source only
# /build/ 目录下的 appicon 和 README 可以提交，bin 目录忽略
# go.sum 需要提交，它是 lockfile

# ── Frontend / Node ───────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/package.json.md5

# ── Wails generated (re-generated by `wails dev`/`build`) ──
# wailsjs 是自动生成的绑定代码，不应手动编辑/提交
# 但社区实践有分歧：提交可让 CI 不依赖 wails 环境
# 这里选择不提交，由 `wails dev` 自动生成
frontend/wailsjs/

# ── OS junk ───────────────────────────────────────────────
Desktop.ini
$RECYCLE.BIN/

# ── Env / Secrets ─────────────────────────────────────────
.env
.env.local
.env.*.local
*.pem
*.key

# ── Logs / Temp ───────────────────────────────────────────
*.log
tmp/
temp/